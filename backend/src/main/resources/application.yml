server:
  port: 8080

spring:
  application:
    name: pv-cases
  jackson:
    default-property-inclusion: non_null
    serialization:
      indent-output: true

logging:
  level:
    com.theragenx: INFO
    org.springframework.web: WARN
